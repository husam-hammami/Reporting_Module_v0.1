# Copy to .env.local — not committed (gitignored), so push/pull won't change your API.
# When set: app uses this backend (e.g. VPN URL). When unreachable, dev app falls back to localhost:5001.
# When unset in dev: app uses http://localhost:5001. Connection always works with or without VPN.
# VITE_API_URL=http://100.118.31.61:5001
