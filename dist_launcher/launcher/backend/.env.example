# Copy to .env and set values for your environment.
# Backend must use the SAME database where you created bins and materials.

POSTGRES_DB=dynamic_db_hercules
POSTGRES_USER=postgres
POSTGRES_PASSWORD=Hercules
DB_HOST=127.0.0.1
DB_PORT=5432

# Optional
# FLASK_SECRET_KEY=your-secret-key
