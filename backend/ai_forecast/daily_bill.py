"""Phase B placeholder — end-of-day OMR projection.

Will compute:
    so_far_today + Σ(remaining_hours × hour_of_day_median × tariff_for_hour)
    p10/p90 from rolling stddev.
"""


def project(asset=None):
    """Phase A: returns None. Phase B populates."""
    return None
