"""Phase B placeholder — shift pace projector.

Will compute:
    produced_so_far / elapsed_h * remaining_h => projected_total
    confidence band ± 1.96 * stddev_30_shifts * sqrt(remaining_h)

Phase A returns None for all assets so the UI can render the "calibrating" empty state.
"""


def project(asset):
    """Phase A: returns None. Phase B populates."""
    return None
